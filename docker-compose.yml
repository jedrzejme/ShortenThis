services:
    yourowncard:
        container_name: shorten-that
        ports:
            - 5000:5000 # <Host Port>:<Container Port (do not change)>
        image: shorten-that
        volumes:
            - ./config.ini:/app/config.ini
            - ./urls.ini:/app/urls.ini
        restart: unless-stopped